import { RotateWheelDirective } from './rotate-wheel.directive';

describe('RotateWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateWheelDirective();
    expect(directive).toBeTruthy();
  });
});
